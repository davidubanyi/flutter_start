import 'package:flutter/material.dart';

class ContributionsHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
    child: Text('The savings page'),
    );
  }
}
